# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | arch | Broker host boundary | Standalone TypeScript broker/runtime, not broker logic embedded inside a `gsd-2` fork | Preserves broker isolation, keeps the runtime reusable across clients, and matches the current Python broker's standalone service boundary. | No | agent |
| D002 | M001 | scope | First milestone scope boundary | M001 focuses on standalone broker parity, durable state, reviewer lifecycle, and typed client work; `gsd-2` workflow gating is deferred to M002 | Keeps the first milestone compatibility-focused and mechanically provable without coupling broker replacement to immediate workflow integration. | Yes — if parity work proves smaller or larger than expected | agent |
| D003 | M001 | integration | Deterministic gate path | Use a direct typed client for deterministic integration paths and keep MCP as a public integration surface | Critical review gates should be runtime-owned rather than dependent on LLM-mediated `mcp_call` behavior. | No | agent |
| D004 | M001 | workflow | First integrated review mode | Keep post-commit review semantics in v1 and target review-before-progression before considering review-before-commit | The current `gsd-2` auto pipeline commits before its verification gate, so review-before-progression is the lowest-risk first integration mode. | Yes — after the standalone broker and typed integration are stable | agent |
| D005 | M001 | library | Dashboard frontend baseline | Treat the existing Astro dashboard in the original repo as the inherited frontend baseline unless later evidence shows it is no longer viable | The old broker repo already captures prior operator UI decisions in Astro, so later dashboard work should recover that direction rather than re-decide it blindly. | Yes — if later implementation constraints invalidate Astro | agent |
| D006 | M003 planning | scope | M003 operator surface boundary | Limit M003 operator-facing work to broker CLI/status and continuity-focused commands that materially help inspection or recovery; defer dashboard/UI work to M004. | The milestone is meant to harden runtime continuity first. Thin runtime-owned command/status surfaces improve trust and operability now without expanding into dashboard restoration, which already has a later owner. | Yes — if continuity proof shows one additional runtime-owned operator command is required for safe recovery. | collaborative |
| D007 | M003 planning | runtime | M003 recovery policy and supervision boundary | Keep reviewer supervision in the broker, auto-recover only clearly safe claimed/stale cases, detach ambiguous open work, record machine-readable recovery reasons durably, and run startup stale-session ownership sweep before normal work resumes. | This preserves recognizable broker semantics from prior art, matches the user’s continuity-first/no-redesign direction, prevents limbo without hiding ambiguity, and keeps the milestone focused on trustworthy runtime behavior rather than a supervisor redesign. | Yes — if execution uncovers a concrete continuity hole that needs a narrow semantic adjustment. | collaborative |
| D008 | M003/S01 planning | architecture | M003/S01 bootstrap strategy in the doc-only worktree | Reconstitute the minimal pnpm/TypeScript/Vitest broker workspace plus `packages/review-broker-core` and `packages/review-broker-server` inside M003 before adding recovery logic, and keep S01 acceptance broker-first through runtime/CLI inspection rather than reopening client, MCP, or dashboard surfaces. | The current M003 worktree has planning artifacts but no runtime implementation to patch. Bootstrapping only the broker substrate needed for continuity proof preserves the already-decided package boundary and keeps the slice focused on real reviewer-exit and stale-claim recovery instead of expanding into a broader redesign. | Yes | agent |
| D009 | M003 S01 T02 | persistence | How to introduce continuity ownership and recovery columns into the broker SQLite schema | Add a new additive migration `004_review_continuity.sql` instead of modifying the existing `001`-`003` migrations | The worktree already contains existing SQLite files and previously-applied migration checksums. Rewriting historical migrations would make checksum validation fail on reopen, including operator inspection databases, while an additive migration preserves restart safety and still extends the schema for session ownership, recovery reason, and action-required state. | Yes | agent |
| D010 | M003/S01/T03 | architecture | How live claim-timeout recovery should execute in the standalone broker runtime | Run claim-timeout recovery opportunistically before public broker service operations, while reviewer exits and startup recovery use the same repository-backed reclaim/detach policy path | The minimal standalone broker runtime in M003 does not need a separate background sweeper process. Running timeout maintenance on broker interactions keeps recovery deterministic in tests, reuses the same transactional continuity helpers as reviewer-exit/startup recovery, and still leaves durable audit evidence without adding another lifecycle loop to supervise. | Yes | agent |
| D011 | M003/S01/T01 | contracts | How M003/S01 should extend broker continuity payloads without breaking existing broker APIs | Keep the new continuity contract additive in review-broker-core so status/timeline/startup recovery payloads extend the shared contract without removing or reshaping existing broker fields first | The standalone broker runtime and tests already consume the existing core contract. Making continuity payloads additive let SQLite/runtime recovery work land incrementally while preserving compatibility for existing operations and test harnesses. | Yes | agent |
| D012 | M003/S01/T04 | api | How broker inspection surfaces should expose continuity data in M003/S01 | Keep `getReviewStatus` additive by preserving the existing `review` summary while attaching ownership, latest recovery, and action-required continuity fields, and add `getReviewTimeline` as the explicit continuity-history surface | Operators and downstream code can adopt continuity inspection without losing the existing status payload shape, while the separate timeline surface gives explicit recovery history instead of overloading one snapshot response. | Yes | agent |
| D013 | M003/S01/T04 | observability | How runtime-wide recovery inspection should be surfaced for continuity verification | Extend `inspectBrokerRuntime()` and CLI `start-broker.ts --once` with continuity snapshot fields instead of building a separate operator-only recovery inspection path | Reusing the supported runtime inspection and CLI once surfaces keeps continuity evidence on the same broker-owned path operators already use, reduces duplicate code, and makes slice acceptance possible without raw SQLite inspection. | Yes | agent |
| D014 | M003/S02 planning | operator-surfaces | How S02 should expose restart continuity inspection | Add one additive runtime continuity inspection operation plus a thin broker-owned CLI wrapper over the existing status/read-model path, instead of overloading generic list responses or adding raw-DB inspection code. | S01 already established the recovery policy and startup sweep ordering. S02's gap is operator visibility. A dedicated continuity contract keeps list payloads stable, gives typed client and MCP callers an explicit restart/ownership inspection surface, and lets the CLI reuse the same broker-owned snapshot without bypassing startup recovery or querying SQLite directly. | Yes — if S03 proves one more runtime-owned continuity surface is needed for milestone acceptance. | agent |
| D015 | M003/S02/T02 | observability | How the new runtime continuity inspection operation should expose reviewer state | Return continuity-safe reviewer snapshots with reviewer IDs, session IDs, current review IDs, status/offline timestamps, and command basenames, but never reviewer argv or raw command text. | The S02 continuity surface must let operators inspect ownership and reviewer state after restart while staying patch-body-safe and argv-safe. Command basenames preserve enough observability to identify the worker process without exposing secret-bearing arguments or full command lines. | Yes | agent |
| D016 | M003/S02/T03 | operator-surfaces | How the S02 continuity CLI should scope and format its output | Keep `inspect-continuity` continuity-focused and redaction-safe by emitting `startupRecovery` plus the typed `inspectRuntimeContinuity()` payload directly, while leaving broader runtime inventory/counts on `start-broker.ts --once`. | Operators need one restart/continuity command that reuses broker-owned state and remains patch-body-safe/argv-safe. Splitting the focused continuity snapshot from the broader once-mode inventory keeps the contract explicit and avoids inventing ad hoc SQLite inspection output. | Yes | agent |
| D017 | M003/S03 planning | acceptance | How S03 should close M003 continuity acceptance | Treat S03 as a test-first integration closer: broaden the existing durable SQLite continuity proof to cover live reviewer exit plus later crash/restart on one database, and keep milestone acceptance on the shipped status/CLI continuity surfaces and repo proof commands instead of adding new inspectors or raw-DB acceptance harnesses. | S01 and S02 already shipped the recovery policy, startup sweep ordering, and continuity inspection surfaces. The remaining milestone gap is assembled proof across real runtime boundaries, so broadening the existing end-to-end proof gives the fastest path to expose any real inconsistency while preserving the decisions to reuse AppContext.close() for crash simulation, keep one shared recovery policy, and verify through broker-owned status/CLI surfaces. | Yes — if the broadened proof exposes a concrete gap that cannot be closed without one additional broker-owned acceptance surface. | agent |
| D018 | M003/S03/T02 | observability | How operator-facing once-mode continuity output should handle reviewer launch details | Sanitize `start-broker.ts --once` `latestReviewer` to expose only basenames and lifecycle metadata, while leaving internal runtime snapshots unchanged. | The assembled S03 operator proof must stay redaction-safe and argv-safe. The CLI is a shipped operator surface, so it should not emit raw command strings or reviewer args, but internal runtime snapshots still need full fidelity for lower-level regression tests and debugging. | Yes | agent |
| D019 | M004 planning | realtime | Live update contract for the dashboard | Treat broker snapshot routes as authoritative truth and use SSE only as liveness/change notification that triggers re-fetch and re-hydration. | The existing VersionedNotificationBus is runtime-local and resets on restart, so using SSE as durable truth would create drift across reconnects and restarts. Snapshot-first re-sync keeps the dashboard aligned with real broker state. | Yes — if later execution adds a durable replayable event stream and the team chooses to expand the realtime contract. | agent |
| D020 | M004 planning | architecture | Dashboard delivery topology for M004 | Serve the dashboard from the broker runtime itself, with a broker-owned HTTP listener mounting the Astro dashboard baseline and exposing broker-owned JSON/SSE routes. | The current codebase has strong broker-side state seams but no browser transport. Mounting the dashboard into the broker preserves the thin-client boundary, keeps the broker as the single source of truth, matches the inherited Astro direction, and avoids inventing a second backend or routing browser behavior through MCP. | Yes — if execution evidence shows the mounted Astro path is not viable in this repo. | agent |
| D021 | M004 planning | realtime | Live update contract for the dashboard | Treat broker snapshot routes as authoritative truth and use SSE only as liveness/change notification that triggers re-fetch and re-hydration. | The existing VersionedNotificationBus is runtime-local and resets on restart, so using SSE as durable truth would create drift across reconnects and restarts. Snapshot-first re-sync keeps the dashboard aligned with real broker state. | Yes — if later execution adds a durable replayable event stream and the team chooses to expand the realtime contract. | agent |
| D022 | M004 planning | observability | First dashboard log/event contract for M004 | Ship a redaction-safe structured operator event feed first, backed by broker audit/runtime evidence, and defer raw reviewer stdout/stderr browsing unless it becomes an explicit later requirement. | Reviewer subprocesses are currently spawned with stdio ignored, so raw subprocess logs do not exist as a reusable runtime surface today. Structured operator events are already aligned with broker truth, durable evidence, and the inspect-first milestone scope. | Yes — if a later requirement explicitly demands raw subprocess log capture. | agent |
| D023 | M004/S01 | architecture | How the M004 dashboard renders content | Fully client-rendered via overview-client.ts rather than Astro server components, since all dashboard content depends on runtime-fetched state not build-time props | The dashboard displays live broker state that changes after every mutation. Server-rendered Astro components would add SSR complexity without benefit since there's no static content to pre-render. Keeping rendering in one client module makes the data flow explicit: fetch snapshot → render panels → SSE triggers re-fetch → re-render. | Yes — if later slices need SEO-friendly or offline-first rendering | agent |
| D024 | M004/S01 | architecture | How the dashboard client consumes shared transport types | Inline a minimal client-side type subset in overview-client.ts rather than importing review-broker-core Zod schemas at Astro build time | Importing review-broker-core at Astro build time would require cross-package TypeScript build coordination and potentially break the Astro bundler's module resolution. The Zod schemas in core remain the authoritative contract enforced by route tests; the client types are a lightweight projection that avoids build coupling. Schema drift is caught by the 14 contract tests and 6 integration tests. | Yes — if a later task adds a type generation step | agent |
| D025 | M004/S02/T01 | contracts | How the event feed route determines hasMore for pagination | getEventFeed() requests limit+1 rows from listGlobal() and uses the extra row's presence to set hasMore, avoiding a separate count query. listGlobal() caps at 201 internally to support this probe at max limit. | A separate COUNT query would double the work per request. The +1 probe is a standard pagination pattern that keeps the query cheap and the hasMore flag accurate without scanning the full table. | Yes | agent |
| D026 | M004/S02/T01 | observability | How event feed responses handle metadata redaction | Strip the entire metadata object from operator event responses. Only the summary string is projected forward; command, args, cwd, and workspaceRoot never appear. The route test stringifies the entire response and asserts absence of both metadata values and key names. | Metadata can contain process paths, reviewer argv, workspace roots, and other operator-unsafe values. Full metadata stripping with explicit summary extraction follows the same projection pattern established by projectLatestReviewer() and ensures no new metadata field can accidentally leak without updating the projection. | Yes — if a future requirement needs specific metadata fields exposed, add them to the explicit projection | agent |
| D027 | M004/S02/T02 | architecture | How event type group filtering works in the dashboard events page | Client-side group filtering (All/Review/Reviewer) in events-client.ts, with the backend providing exact eventType filtering only. | The event feed route supports exact eventType string matching, not prefix queries. Adding prefix support to the backend would complicate the SQL layer for a filter that works well enough client-side given the default 50-event page size. If event volume grows, the route can be extended with prefix support later. | Yes — if event volume makes client-side filtering impractical | agent |
| D028 | M004/S03/T01 | architecture | How review routes access broker service methods in dashboard route handler | Added BrokerService as an explicit required field on DashboardRouteDependencies rather than reaching through AppContext | Keeps route handlers decoupled from internal AppContext wiring. The broker service is the correct public read surface for review data, and making it an explicit dependency prevents routes from reaching into runtime internals. All existing callers (CLI, 3 test files, 22 call sites) were updated to pass service: runtime.service. | Yes | agent |
